pipeline {
  agent any

  tools {
    maven 'Maven 3.8.7'
    jdk 'JDK 17'
  }

  environment {
    SONARQUBE = 'SonarQube'
    DOCKER_IMAGE = 'venkatesh384/jenkins-demo'
    IMAGE_TAG = "${BUILD_NUMBER}"
    NGINX_IMAGE = 'venkatesh384/nginx-reverse-proxy'
  }

  stages {

    stage('📥 Checkout') {
      steps {
        git 'https://github.com/venky2350/Springboot-cicd-on-k8s.git'
      }
    }

    stage('🔨 Maven Build') {
      steps {
        dir('app') {
          sh 'mvn clean package'
        }
      }
    }

    stage('🔎 SonarQube Analysis') {
      steps {
        dir('app') {
          withSonarQubeEnv("${SONARQUBE}") {
            withCredentials([string(credentialsId: 'sonarqube-token', variable: 'SONAR_TOKEN')]) {
              sh '''
                set -e
                mvn clean verify sonar:sonar \
                  -Dsonar.projectKey=Springboot-cicd-on-k8s \
                  -Dsonar.projectName='Springboot-cicd-on-k8s' \
                  -Dsonar.host.url=http://54.162.210.95:9000 \
                  -Dsonar.token=$SONAR_TOKEN
              '''
            }
          }
        }
      }
    }

    stage('📄 Generate Sonar HTML Report') {
      steps {
        dir('app') {
          sh '''
            echo "📄 Generating HTML report using sonar-report plugin..."
            mvn verify -Pgenerate-sonar-html
          '''
        }
      }
    }

    stage('🐳 Docker Build & Push') {
      steps {
        withCredentials([usernamePassword(
          credentialsId: 'dockerhub-creds',
          usernameVariable: 'DOCKER_USERNAME',
          passwordVariable: 'DOCKER_PASSWORD'
        )]) {
          dir('app') {
            sh '''
              echo "🔐 Logging in to Docker Hub..."
              echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin

              echo "🐳 Building Docker image..."
              docker build -t $DOCKER_IMAGE:latest .

              echo "📤 Pushing Docker image to Docker Hub..."
              docker push $DOCKER_IMAGE:latest
            '''
          }
        }
      }
    }

    stage('🔐 Trivy Security Scan') {
      steps {
        dir('trivy') {
          withEnv(["IMAGE_NAME=${DOCKER_IMAGE}:latest"]) {
            sh '''
              set -e
              if command -v trivy >/dev/null 2>&1; then
                echo "🔎 Running Trivy security scan on $IMAGE_NAME..."
                ./scan-trivy.sh
              else
                echo "⚠️ Trivy not found – skipping image scan. Install it inside Jenkins container."
              fi
            '''
          }
        }
      }
    }

    stage('📄 Copy Reports') {
      steps {
        sh '''
          mkdir -p reports
          cp trivy-reports/image-scan-report.txt reports/trivy-report.txt || echo "⚠️ No Trivy text report found."
          cp trivy-reports/report.json reports/trivy-report.json || echo "⚠️ No Trivy JSON report found."
          cp app/target/sonar-report/index.html reports/sonar-report.html || echo "⚠️ No Sonar HTML report found."
          cp reports/image-scan.json reports/image-scan.json || echo "⚠️ No external image-scan.json found."
        '''
        archiveArtifacts artifacts: 'reports/**', allowEmptyArchive: true
      }
    }

    stage('🚀 Deploy with Helm') {
      agent {
        docker {
          image 'alpine/helm:3.14.4'
        }
      }
      steps {
        dir('jenkins-demo-chart') {
          sh '''
            set -e
            echo "🔧 Installing/Upgrading Helm chart..."

            helm upgrade --install jenkins-demo . \
              --namespace dev --create-namespace \
              --set springApp.image=venkatesh384/jenkins-demo:latest \
              --set nginx.image=venkatesh384/nginx-reverse-proxy:latest

            echo "⏳ Waiting for rollout to complete..."
            kubectl rollout status deployment/spring-app -n dev

            echo "📊 Helm deployment done. Fetching resources..."
            kubectl get all -n dev
          '''
        }
      }
    }

    stage('🧱 Nginx Docker Build & Push') {
      steps {
        withCredentials([usernamePassword(
          credentialsId: 'dockerhub-creds',
          usernameVariable: 'DOCKER_USERNAME',
          passwordVariable: 'DOCKER_PASSWORD'
        )]) {
          dir('nginx') {
            sh '''
              echo "🔐 Logging in to Docker Hub for Nginx image..."
              echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin

              echo "✅ Checking required files..."
              [ -f default.conf ] || { echo "❌ default.conf missing!"; exit 1; }
              [ -f index.html ] || { echo "❌ index.html missing!"; exit 1; }
              [ -f reports/sonar-report.html ] || {
                echo "⚠️ sonar-report.html not found. Creating a placeholder..."
                mkdir -p reports
                echo "<html><body><h2>No Sonar Report Found</h2></body></html>" > reports/sonar-report.html
              }

              echo "🧱 Building Nginx Docker image..."
              docker build -t $NGINX_IMAGE:latest .

              echo "📤 Pushing Nginx image to Docker Hub..."
              docker push $NGINX_IMAGE:latest
            '''
          }
        }
      }
    }
  }

  post {
    always {
      echo "🧹 Cleaning workspace..."
      cleanWs()
    }
  }
}
