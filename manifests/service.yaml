apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: springboot-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
