# Step into the nginx directory
cd nginx/

# Build the Nginx Docker image
docker build -t venkatesh384/nginx-reverse-proxy:latest .

# Push the image to Docker Hub
docker push venkatesh384/nginx-reverse-proxy:latest
